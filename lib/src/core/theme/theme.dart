import 'package:flutter/material.dart';

class AppTheme {
  static ThemeData themeCollection = ThemeData(
    scaffoldBackgroundColor: Colors.white,
    primaryColor: const Color(0xFF9b1010),
    appBarTheme: const AppBarTheme(color: Color(0xFF9b1010)),
    primarySwatch: Colors.blue,
  );
}
